//TODO cgi write to pipe doesn't write body
cgit read should read until 0 in a loop
resolveCgiScript should return on error
script header typos

// IMPORTANT!!! Apparenlty, I've got a BLOCKING server, I should separate EPOLLIN and EPOLLOUT logic.

//TODO kill() cgi script in case client hung up during script execution
handle pid_t child in the cgiInfo -> kinda done, gotta code review

//TODO parse cgi script response and queue it to the epoll -> kinda done, gotta code review

//TODO add reponse queue to the client -> kinda done, gotta code review

//TODO add client state and implement it into checkTimeouts() for the CGI scripts

//TODO WIP -> move request path normalization to parser from the handler. -> 
//TODO add intermediate step router which concatenate req path with loc path = phys path and passes it to handler to abstract it more
change the way limit_except is parsed and stored, use int bitmask to optimize the comparison

//TODO WIP split pasrsing of headers and body into 2 steps

add scrip to explicitly create files with POST

// TODO no need to normalize path in router, only cat to match phys

// 100 continue header when uploading file larger than 1024 bytes

POST AND DELETE METHODS HANDLE, SO FAR ONLY GET IS HANDLED |||| support of CGI for these methods later

for POST:
1) check limit_except, if not allowed send 405 with allowed METHODS
2) create/modify file
3) send page "successfully_uploaded.html" with GET?

File System Hierarhy Standard for Server in docker/cmake

KEEP ALIVE TIMER!!! -> works, might've forgotten something

handle cgi timeouts

remove exits(Failure) inside server loop, disconnect client instead
make tokenize() ignore comments

handle if index not found, autoindex off TODO as well as multiple indexes

to prevent malicious user, send error 400 in case of same headers in request

parse headers for the cgi

check if happens if the client runs CGI and instantly sends totally different request (potentially should save current request state in cgi)

store IPV4 or IPV6 in Client, also pass it to the CGI (inet_ntop  #include <arpa/inet.h>)

add extern for c libs

move cgiinfo from client to httprequest makes more sense ig

potentially dynamically allocate memore if keep_alive in CLient.cpp

CGI
1. check if request Content-Type is png and the method is POST
2. check config location for cgi rule
3. check physical address of the cgi script in the config
4. fork() + pipe() server process
5. set environment variables and run execve()
6. run open() with "xb" flags
7. for now create files in the static ./images/ dir

make cleanup as well as logs look pretty (remove unnecessary std::couts)